apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-awesome-block-volume
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: sbs-default
  volumeMode: Block
